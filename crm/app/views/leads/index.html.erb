<h1>Leads#index</h1>
<p>Find me in app/views/leads/index.html.erb</p>

<%= link_to 'New Lead', new_lead_path %>

<% @leads.each do |lead| %>
  <p><%= link_to lead.first_name, lead_url(lead) %> </p>
<% end %>
